{
    "tasks": [
        {
            "type": "shell",
            "label": "build printf_test",
            "command": "/usr/bin/make",
            "args": [
                "-C",
                "${workspaceFolder}",
                "re",
                "test"
            ],
            "options": {
                "cwd": "/usr/bin",
                "env": {
                    "DEBUG" : "-g"
                }
            }
        }
    ],
    "version": "2.0.0"
}
